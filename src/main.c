#include <SDL2/SDL.h>
#include <SDL2/SDL_image.h>
#include <SDL2/SDL_mixer.h>
#include <stdio.h>



#define WINDOW_WIDTH (640)
#define WINDOW_HEIGHT (480)

int main() {

    int init();
    int loadMedia();
    int loop();
    void close();

    return 0;
}
